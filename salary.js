function salary(hoursWorked, overTime){
    console.log((1000 * hoursWorked) + (200 * overTime))
};

salary(150, 50)//where hours worked will be inputed for the overall result of salary at the end of the month
